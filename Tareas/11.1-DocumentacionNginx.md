# 🌐 Tarea 11.1: Nginx para Aplicaciones Web

## 📋 **Índice**

1. [🤔 ¿Qué es Nginx?](#-qué-es-nginx)
2. [🔍 ¿Por qué Nginx para Angular/React/Vue?](#-por-qué-nginx-para-angularreactvue)
3. [🆚 Nginx vs Apache vs Node.js](#-nginx-vs-apache-vs-nodejs)
4. [📐 Arquitectura: Aplicación Angular + Nginx](#-arquitectura-aplicación-angular--nginx)
5. [🎯 El Problema de las SPA (Single Page Applications)](#-el-problema-de-las-spa-single-page-applications)
6. [⚙️ Configuración Básica de nginx.conf](#️-configuración-básica-de-nginxconf)
7. [🔧 Directivas Importantes de Nginx](#-directivas-importantes-de-nginx)
8. [🚀 Optimizaciones de Performance](#-optimizaciones-de-performance)
9. [🔒 Seguridad con Nginx](#-seguridad-con-nginx)
10. [🔄 Proxy Reverso: Conectar Frontend con Backend](#-proxy-reverso-conectar-frontend-con-backend)
11. [🐳 Nginx en Docker](#-nginx-en-docker)
12. [📊 Casos de Uso Reales](#-casos-de-uso-reales)
13. [🛠️ Debugging y Logs](#️-debugging-y-logs)

---

## 🤔 **¿Qué es Nginx?**

**Nginx** (pronunciado "engine-x") es un **servidor web de alto rendimiento** que puede actuar como:

1. **Servidor Web:** Sirve archivos estáticos (HTML, CSS, JS, imágenes)
2. **Proxy Reverso:** Redirige peticiones a otros servidores
3. **Load Balancer:** Distribuye tráfico entre múltiples servidores
4. **Cache Server:** Almacena respuestas para mejorar velocidad

### **¿Por qué es tan popular?**

| Característica | Descripción |
|----------------|-------------|
| 🚀 **Rápido** | Maneja 10,000+ conexiones simultáneas con poco consumo de RAM |
| 🪶 **Ligero** | Imagen Docker de solo ~23MB (Alpine) |
| ⚡ **Eficiente** | Arquitectura asíncrona y basada en eventos |
| 🔧 **Configurable** | Flexible y potente mediante archivos .conf |

---

## 🔍 **¿Por qué Nginx para Angular/React/Vue?**

### **El flujo de una aplicación Angular:**

```
1. Desarrollo:
   npm run build → Compila TypeScript/Angular → dist/
   
2. Resultado:
   dist/
   ├── index.html        ← Punto de entrada
   ├── main.js           ← Código de la aplicación (compilado)
   ├── polyfills.js      ← Compatibilidad navegadores
   ├── runtime.js        ← Angular runtime
   └── assets/           ← Imágenes, fuentes, etc.

3. Problema:
   Estos archivos NO se ejecutan solos ❌
   Necesitan un servidor web ✅
```

### **¿Por qué NO usar otros servidores?**

| Servidor | ¿Sirve archivos? | Tamaño | Velocidad | ¿Recomendado? |
|----------|------------------|--------|-----------|---------------|
| **Nginx** | ✅ | ~23MB | ⚡⚡⚡ | ✅ SÍ (Producción) |
| **Apache** | ✅ | ~180MB | ⚡⚡ | ⚠️ Más pesado |
| **Node.js (Express)** | ✅ | ~300MB+ | ⚡ | ❌ Overkill para estáticos |
| **Python (HTTP Server)** | ✅ | Variable | ⚡ | ❌ Solo desarrollo |

**Conclusión:** Nginx es el más ligero y rápido para servir archivos estáticos.

---

## 🆚 **Nginx vs Apache vs Node.js**

### **Comparación Técnica:**

| Aspecto | Nginx | Apache | Node.js |
|---------|-------|--------|---------|
| **Arquitectura** | Asíncrono/Event-driven | Thread por conexión | Asíncrono/Event-driven |
| **Consumo RAM** | Bajo (~10MB) | Alto (~50MB+) | Medio (~30MB+) |
| **Conexiones simultáneas** | 10,000+ | ~1,000 | Variable |
| **Archivos estáticos** | ⚡⚡⚡ Excelente | ⚡⚡ Bueno | ⚡ Regular |
| **Configuración** | nginx.conf | .htaccess/httpd.conf | Código JavaScript |
| **Uso típico** | Producción | Hosting compartido | Backend APIs |

### **¿Cuándo usar cada uno?**

```
✅ Nginx:
   - Aplicaciones Angular/React/Vue (archivos estáticos)
   - Proxy reverso para APIs
   - Alta concurrencia
   - Producción con Docker

✅ Apache:
   - Hosting compartido tradicional
   - Aplicaciones PHP legacy
   - .htaccess necesario

✅ Node.js:
   - APIs REST/GraphQL
   - Server-side rendering (Next.js, Nuxt.js)
   - WebSockets en tiempo real
```

---

## 📐 **Arquitectura: Aplicación Angular + Nginx**

### **Flujo completo:**

```
┌─────────────────────────────────────────────────────────────┐
│                    DESARROLLO (Tu máquina)                   │
├─────────────────────────────────────────────────────────────┤
│  1. Escribes código Angular (TypeScript)                    │
│  2. npm run build --configuration production                │
│  3. Angular CLI compila → dist/                             │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    DOCKERFILE (Multi-Stage)                  │
├─────────────────────────────────────────────────────────────┤
│  Stage 1 (node:18-alpine):                                  │
│    - Instala dependencias (npm ci)                          │
│    - Compila aplicación (npm run build)                     │
│    - Resultado: dist/ (archivos estáticos)                  │
│                                                              │
│  Stage 2 (nginx:alpine):                                    │
│    - Copia dist/ desde Stage 1                              │
│    - Copia nginx.conf                                       │
│    - Imagen final: ~50MB                                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    CONTENEDOR EN EJECUCIÓN                   │
├─────────────────────────────────────────────────────────────┤
│  Nginx escucha en puerto 80                                 │
│  /usr/share/nginx/html/ ← dist/                             │
│                                                              │
│  Usuario → http://localhost/                                │
│         ↓                                                    │
│  Nginx sirve index.html                                     │
│         ↓                                                    │
│  Navegador carga JavaScript                                 │
│         ↓                                                    │
│  Angular Router toma control de las rutas                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 **El Problema de las SPA (Single Page Applications)**

### **¿Qué es una SPA?**

Una **Single Page Application** (Angular, React, Vue) carga **UN SOLO HTML** y usa JavaScript para cambiar el contenido dinámicamente.

### **El Problema:**

```
Usuario abre:     http://localhost/
                  ↓
Navegador pide:   GET / HTTP/1.1
                  ↓
Nginx responde:   index.html ✅
                  ↓
Angular carga y funciona ✅

Usuario navega:   /owners (Angular Router cambia la URL en el navegador)
                  Todo funciona ✅ (JavaScript ya está cargado)

Usuario RECARGA:  F5 en http://localhost/owners
                  ↓
Navegador pide:   GET /owners HTTP/1.1
                  ↓
Nginx busca:      /usr/share/nginx/html/owners.html
                  ↓
ERROR 404:        Archivo no existe ❌
```

### **La Solución: try_files**

```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

**Traducción:**
1. `$uri` → Intenta servir el archivo exacto (ej: `logo.png`)
2. `$uri/` → Si no existe, intenta como directorio
3. `/index.html` → Si nada funciona, sirve `index.html` (Angular se encarga)

**Resultado:**
```
GET /owners → Nginx sirve index.html
           → Angular detecta ruta "/owners"
           → Angular muestra OwnersComponent
           → ✅ Funciona!
```

---

## ⚙️ **Configuración Básica de nginx.conf**

### **Estructura de un archivo nginx.conf:**

```nginx
server {
    # ============================================
    # 1. CONFIGURACIÓN BÁSICA
    # ============================================
    listen 80;                    # Puerto donde escucha Nginx
    server_name localhost;         # Dominio (puede ser example.com)
    root /usr/share/nginx/html;    # Directorio de archivos
    index index.html;              # Archivo por defecto

    # ============================================
    # 2. RUTAS (SPA)
    # ============================================
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ============================================
    # 3. LOGS
    # ============================================
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
}
```

### **Explicación de cada sección:**

| Directiva | Descripción | Ejemplo |
|-----------|-------------|---------|
| `listen` | Puerto donde escucha Nginx | `listen 80;` (HTTP) o `listen 443 ssl;` (HTTPS) |
| `server_name` | Dominio del servidor | `localhost`, `example.com`, `*.example.com` |
| `root` | Directorio raíz de archivos | `/usr/share/nginx/html` |
| `index` | Archivo por defecto | `index.html`, `index.htm` |
| `location` | Bloque de configuración por ruta | `location / { ... }` |
| `try_files` | Intenta servir archivos en orden | `try_files $uri $uri/ /index.html;` |

---

## 🔧 **Directivas Importantes de Nginx**

### **1. listen - Puerto de Escucha**

```nginx
# HTTP (puerto 80)
listen 80;

# HTTPS (puerto 443)
listen 443 ssl;
ssl_certificate /etc/nginx/ssl/cert.pem;
ssl_certificate_key /etc/nginx/ssl/key.pem;

# IPv6
listen [::]:80;

# Múltiples puertos
listen 80;
listen 8080;
```

---

### **2. location - Configuración por Ruta**

```nginx
# Todas las rutas
location / {
    try_files $uri $uri/ /index.html;
}

# Ruta específica
location /api {
    proxy_pass http://backend:8080;
}

# Expresiones regulares (archivos estáticos)
location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
    expires 1y;
}

# Coincidir exactamente
location = /health {
    return 200 "OK";
}
```

---

### **3. try_files - Servir Archivos**

```nginx
# SPA: Siempre sirve index.html si no existe el archivo
try_files $uri $uri/ /index.html;

# Con fallback personalizado
try_files $uri $uri/ /404.html;

# Múltiples fallbacks
try_files $uri $uri/ /index.php?$args;
```

---

### **4. root vs alias**

```nginx
# root: Añade la ruta al location
location /images {
    root /usr/share/nginx/html;
    # GET /images/logo.png → /usr/share/nginx/html/images/logo.png
}

# alias: Reemplaza la ruta del location
location /images {
    alias /usr/share/nginx/html/assets;
    # GET /images/logo.png → /usr/share/nginx/html/assets/logo.png
}
```

---

### **5. add_header - Headers HTTP**

```nginx
# Cache control
add_header Cache-Control "public, max-age=31536000";

# Seguridad
add_header X-Frame-Options "SAMEORIGIN";
add_header X-Content-Type-Options "nosniff";
add_header X-XSS-Protection "1; mode=block";

# CORS (si necesitas acceso desde otros dominios)
add_header Access-Control-Allow-Origin "*";
```

---

## 🚀 **Optimizaciones de Performance**

### **1. Gzip - Compresión**

```nginx
# Habilitar gzip
gzip on;
gzip_comp_level 6;           # Nivel de compresión (1-9, 6 es óptimo)
gzip_min_length 1000;        # No comprimir archivos < 1KB
gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

# Resultado: Archivos 70% más pequeños
# main.js (500KB) → main.js.gz (150KB) ✅
```

---

### **2. Cache - Almacenar en Navegador**

```nginx
# Cache para archivos estáticos (1 año)
location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
    access_log off;  # No loguear (optimización)
}

# NO cache para index.html (siempre última versión)
location = /index.html {
    add_header Cache-Control "no-cache, no-store, must-revalidate";
    add_header Pragma "no-cache";
    add_header Expires "0";
}
```

**¿Por qué?**
- **JS/CSS:** Tienen hash en el nombre (`main.abc123.js`) → Si cambia el código, cambia el nombre
- **index.html:** No tiene hash → Debe verificarse siempre

---

### **3. Buffering y Timeouts**

```nginx
# Aumentar buffers para peticiones grandes
client_body_buffer_size 10K;
client_header_buffer_size 1k;
client_max_body_size 8m;

# Timeouts
client_body_timeout 12;
client_header_timeout 12;
keepalive_timeout 15;
send_timeout 10;
```

---

## 🔒 **Seguridad con Nginx**

### **Headers de Seguridad Básicos:**

```nginx
# Prevenir clickjacking (tu app en un iframe)
add_header X-Frame-Options "SAMEORIGIN" always;

# Prevenir MIME type sniffing
add_header X-Content-Type-Options "nosniff" always;

# Activar protección XSS del navegador
add_header X-XSS-Protection "1; mode=block" always;

# Content Security Policy (CSP)
add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline';" always;

# HTTPS Strict Transport Security (solo si usas HTTPS)
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
```

---

### **Ocultar Versión de Nginx:**

```nginx
# En nginx.conf (nivel http)
server_tokens off;

# Resultado:
# Antes: Server: nginx/1.21.6
# Después: Server: nginx
```

---

### **Limitar Rate (prevenir ataques DDoS):**

```nginx
# Definir zona de límite (10MB para almacenar IPs)
limit_req_zone $binary_remote_addr zone=one:10m rate=10r/s;

server {
    location / {
        # Máximo 10 peticiones por segundo por IP
        limit_req zone=one burst=20;
        try_files $uri $uri/ /index.html;
    }
}
```

---

## 🔄 **Proxy Reverso: Conectar Frontend con Backend**

### **Escenario:**

Tu aplicación Angular hace peticiones a una API:

```javascript
// Angular service
this.http.get('http://backend:8080/api/owners')
```

**Problema:** El navegador no puede acceder directamente a `http://backend:8080` (CORS, red Docker interna).

**Solución:** Proxy reverso en Nginx.

---

### **Configuración:**

```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Frontend (Angular)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Backend API (Proxy)
    location /api {
        # Reescribe /api/owners → /owners en el backend
        rewrite ^/api(/.*)$ $1 break;
        
        # Redirige al backend
        proxy_pass http://backend:8080;
        
        # Headers necesarios
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Cache bypass
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Flujo:**

```
Usuario → http://localhost/api/owners
       ↓
Nginx recibe petición
       ↓
Nginx reescribe: /api/owners → /owners
       ↓
Nginx envía: http://backend:8080/owners
       ↓
Backend responde con datos JSON
       ↓
Nginx devuelve respuesta al usuario
       ↓
Angular procesa los datos ✅
```

---

## 🐳 **Nginx en Docker**

### **Dockerfile con Nginx:**

```dockerfile
FROM nginx:alpine

# Copiar archivos de la app
COPY dist/my-app /usr/share/nginx/html

# Copiar configuración personalizada
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
```

---

### **Comandos Útiles:**

```bash
# Entrar al contenedor
docker exec -it <container_name> sh

# Ver archivos servidos
ls /usr/share/nginx/html/

# Ver configuración
cat /etc/nginx/conf.d/default.conf

# Recargar configuración (sin reiniciar)
docker exec <container_name> nginx -s reload

# Ver logs en tiempo real
docker logs -f <container_name>

# Test de configuración
docker exec <container_name> nginx -t
```

---

## 📊 **Casos de Uso Reales**

### **1. SPA Simple (Angular, React, Vue)**

```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

---

### **2. SPA + API Backend**

```nginx
server {
    listen 80;
    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://backend:8080;
    }
}
```

---

### **3. Múltiples SPAs (Monorepo)**

```nginx
server {
    listen 80;

    # App 1: Dashboard
    location /dashboard {
        alias /usr/share/nginx/html/dashboard;
        try_files $uri $uri/ /dashboard/index.html;
    }

    # App 2: Admin
    location /admin {
        alias /usr/share/nginx/html/admin;
        try_files $uri $uri/ /admin/index.html;
    }
}
```

---

### **4. Microservicios**

```nginx
server {
    listen 80;

    location /users {
        proxy_pass http://users-service:3001;
    }

    location /products {
        proxy_pass http://products-service:3002;
    }

    location /orders {
        proxy_pass http://orders-service:3003;
    }
}
```

---

## 🛠️ **Debugging y Logs**

### **1. Ver Logs en Tiempo Real:**

```bash
# Dentro del contenedor
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# Desde Docker
docker logs -f <container_name>
```

---

### **2. Test de Configuración:**

```bash
# Validar sintaxis de nginx.conf
nginx -t

# Desde Docker
docker exec <container_name> nginx -t
```

---

### **3. Recargar Configuración sin Reiniciar:**

```bash
# Dentro del contenedor
nginx -s reload

# Desde Docker
docker exec <container_name> nginx -s reload
```

---

### **4. Debugging de Rutas:**

```nginx
# Añadir header para debug
location / {
    add_header X-Debug-Uri "$uri" always;
    add_header X-Debug-Request "$request" always;
    try_files $uri $uri/ /index.html;
}
```

Luego inspecciona las respuestas HTTP en el navegador (DevTools → Network → Headers).

---

## 🎯 **Resumen Rápido**

| Concepto | Explicación | Uso en Petclinic |
|----------|-------------|------------------|
| **Nginx** | Servidor web ligero y rápido | Servir Angular compilado |
| **SPA** | App de una sola página (Angular) | Necesita `try_files` |
| **try_files** | Siempre sirve index.html | Rutas de Angular funcionan al recargar |
| **gzip** | Comprime archivos | Reduce tamaño 70% |
| **Cache** | Almacena en navegador | JS/CSS se cargan más rápido |
| **Proxy** | Redirige peticiones | Conectar Angular con API Java |
| **Multi-Stage** | Build en node, sirve con nginx | Imagen final 50MB vs 600MB |

---

## 📚 **Referencias**

- [Documentación oficial de Nginx](https://nginx.org/en/docs/)
- [Nginx Docker Hub](https://hub.docker.com/_/nginx)
- [Nginx Beginner's Guide](https://nginx.org/en/docs/beginners_guide.html)

---

**Documentación creada:** Octubre 2025  
**Última actualización:** Octubre 2025  
**Versión:** 1.0
